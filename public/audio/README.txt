Place your audio files in this directory.

Required files:
- sample.mp3 (default audio file for testing)

The audio files in this directory will be accessible via URLs like:
http://localhost:xxxx/audio/sample.mp3

Note: Make sure to use appropriate audio formats (mp3, wav, ogg) that are supported by modern browsers. 